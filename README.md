# Password Manager

A desktop password manager built with Python and Tkinter.

## Features

- Generate strong random passwords (letters, numbers, and symbols)
- Automatically copy generated passwords to clipboard
- Save website credentials (website, email, password) to a JSON file
- Confirmation dialog before saving
- Input validation to prevent empty fields

## How to Run

```bash
pip install pyperclip
python main.py
```